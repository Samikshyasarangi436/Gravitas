// This is just a function that returns a jsx component which is exported at the bottom

const Navbar = () => {
    return (
        <nav className="navbar">
            
        </nav>

    );
}
 
export default Navbar ;